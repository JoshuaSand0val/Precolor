// Palette: Curated palette of colors suitable for theming.

import { blue, gray, green, red, yellow, cyan, white, black } from "./colors.js";

export const primary = blue;
export const secondary = gray;
export const success = green;
export const danger = red;
export const warning = yellow;
export const info = cyan;
export const light = white;
export const dark = black;

export const colorPalette = {
	primary,
	secondary,
	success,
	danger,
	warning,
	info,
	light,
	dark
};